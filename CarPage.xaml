<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="RentACar_Mobile_.CarPage"
             Title="CarPage">
    <StackLayout Margin="20">
        <Editor Placeholder="Enter the name of the luxury car"
                Text="{Binding Name}"
                HeightRequest="100" />
        <Editor Placeholder="Enter some details that you want for your luxury car"
                Text="{Binding Description}"
                HeightRequest="100" />
        <Editor Placeholder="Enter the type of the car you want"
                Text="{Binding Collection}"
                HeightRequest="100" />
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button Text="Save"
                    Clicked="OnSaveButtonClicked" />
            <Button Grid.Column="1"
                    Text="Delete"
                    Clicked="OnDeleteButtonClicked" />
        </Grid>
    </StackLayout>
</ContentPage>